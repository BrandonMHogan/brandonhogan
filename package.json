{
  "name": "brandonhogan.com",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "devDependencies": {
    "typescript": "latest",
    "vite": "latest",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  }
}
